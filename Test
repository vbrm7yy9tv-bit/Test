<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>For Keravy 💕</title>

  <style>
    body {
      margin: 0;
      padding: 0;
      background: linear-gradient(to bottom right, #ffd6e8, #ffeef7);
      font-family: "Comic Sans MS", cursive, sans-serif;
      text-align: center;
      height: 100vh;
      overflow: hidden;
    }

    h1 {
      margin-top: 100px;
      font-size: 2.5rem;
      color: #e6005c;
      text-shadow: 1px 1px 2px rgba(0,0,0,0.1);
    }

    h2 {
      font-size: 1.5rem;
      color: #ff3385;
      margin-top: 10px;
    }

    .heart {
      font-size: 90px;
      margin-top: 50px;
      animation: beat 1s infinite;
    }

    @keyframes beat {
      0% { transform: scale(1); }
      50% { transform: scale(1.2); }
      100% { transform: scale(1); }
    }

    button {
      margin: 25px;
      padding: 15px 40px;
      font-size: 22px;
      border: none;
      border-radius: 15px;
      cursor: pointer;
      transition: 0.2s;
    }

    #yesBtn {
      background: #ff3366;
      color: white;
      box-shadow: 0px 5px 15px rgba(255, 51, 102, 0.4);
    }

    #yesBtn:hover {
      transform: scale(1.08);
    }

    #noBtn {
      background: #777;
      color: white;
      position: absolute;
    }

    .floating-hearts {
      position: absolute;
      width: 100%;
      height: 100%;
      overflow: hidden;
      top: 0;
      left: 0;
      pointer-events: none;
    }

    .floating-hearts span {
      position: absolute;
      bottom: -20px;
      font-size: 25px;
      animation: floatUp 6s linear infinite;
      opacity: 0.7;
    }

    @keyframes floatUp {
      0% { transform: translateY(0); opacity: 0.8; }
      100% { transform: translateY(-120vh); opacity: 0; }
    }
  </style>
</head>

<body>

  <div class="floating-hearts" id="hearts"></div>

  <div class="heart">💘</div>
  <h1>Keravy...</h1>
  <h2>Will you be my Valentine? 💕</h2>

  <button id="yesBtn">Yes 😍</button>
  <button id="noBtn">No 😭</button>

  <script>
    const noBtn = document.getElementById("noBtn");

    // Set initial position for No button
    noBtn.style.left = "55%";
    noBtn.style.top = "70%";

    noBtn.addEventListener("mouseover", () => {
      const x = Math.random() * (window.innerWidth - 150);
      const y = Math.random() * (window.innerHeight - 80);

      noBtn.style.left = x + "px";
      noBtn.style.top = y + "px";
    });

    document.getElementById("yesBtn").addEventListener("click", () => {
      document.body.innerHTML = `
        <div style="margin-top:150px;">
          <h1 style="font-size:3rem; color:#e6005c;">YAYYY!! 😭💖</h1>
          <h2 style="font-size:1.8rem; color:#ff3385;">
            Keravy, you just made me the happiest person 😍🌸
          </h2>
          <div style="font-size:100px; margin-top:30px;">❤️🥰❤️</div>
          <p style="font-size:1.2rem; color:#cc0052; margin-top:20px;">
            Now you’re officially my Valentine 💕
          </p>
        </div>
      `;
    });

    // Floating hearts animation
    const heartsContainer = document.getElementById("hearts");

    function createHeart() {
      const heart = document.createElement("span");
      heart.innerHTML = "💗";
      heart.style.left = Math.random() * 100 + "vw";
      heart.style.animationDuration = (Math.random() * 3 + 3) + "s";
      heartsContainer.appendChild(heart);

      setTimeout(() => {
        heart.remove();
      }, 6000);
    }

    setInterval(createHeart, 300);
  </script>

</body>
</html>
